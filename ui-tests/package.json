{
  "name": "ipyopenlayers-ui-tests",
  "version": "1.0.0",
  "description": "ipyopenlayers UI Tests",
  "private": true,
  "scripts": {
    "start": "jupyter lab --config jupyter_server_config.py",
    "test": "playwright test --browser chromium",
    "test:update": "playwright test --browser chromium--update-snapshots",
    "test:debug": "PWDEBUG=1 playwright test --browser chromium"
  },
  "author": "ipyopenlayers",
  "license": "Apache-2.0",
  "devDependencies": {
    "@jupyterlab/galata": "^5.0.1",
    "@playwright/test": "^1.32.0"
  },
  "dependencies": {
    "@types/klaw-sync": "^6.0.1",
    "klaw-sync": "^6.0.0"
  }
}
